import { GetValueByPathPipe } from './get-value-by-path.pipe';

describe('GetValueByPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueByPathPipe();
    expect(pipe).toBeTruthy();
  });
});
